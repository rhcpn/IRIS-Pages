let $constants = {
  LOADER_CONFIG: {
    color: "#1192e8",
    width: 80,
    height: 80,
    zIndex: 8200,
    loader: "spinner",
    canCancel: true,
  }
}

export default Object.freeze($constants);
